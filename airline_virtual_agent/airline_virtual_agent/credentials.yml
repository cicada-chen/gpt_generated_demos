"""
# This file contains the credentials for the airline's booking and flight status systems.
# The exact details would depend on the specific systems used by the airline, but here is a general example.

# Rasa
rasa:
  url: "http://localhost:5002/api"

# Rest API
rest:
  # you don't need to provide anything here - this channel doesn't
  # require any credentials

# Airline Booking System
airline_booking:
  url: "http://airline_booking_system_url"
  api_key: "your_airline_booking_system_api_key"

# Airline Flight Status System
airline_flight_status:
  url: "http://airline_flight_status_system_url"
  api_key: "your_airline_flight_status_system_api_key"
"""
